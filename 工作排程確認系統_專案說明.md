# 工作排程確認系統 — 專案說明文件

> 本文件用於換機或開新對話時交接使用。將此 MD 檔與 `public/index.html` 一起上傳到新的 Claude 對話，即可請 Claude 接續開發或修改。若要連後端一起交接，整個 repo 都在 GitHub 上。

---

## 檔案

- **主程式**：`public/index.html`（單一 HTML 檔，內含 CSS + JavaScript，可直接雙擊在瀏覽器開啟，或作為 Claude Artifact 使用）
- **本說明檔**：`工作排程確認系統_專案說明.md`；部署步驟見 `README.md`
- **後端**（選用，部署到 Cloudflare 才需要）：

  | 檔案 | 內容 |
  |---|---|
  | `public/login.html` | 登入／註冊頁（含 Turnstile） |
  | `public/admin.html` | 帳號管理頁（僅管理者） |
  | `src/index.js` | 路由與存取控制 |
  | `src/crypto.js` | PBKDF2 密碼雜湊、token 產生 |
  | `src/session.js` | session 建立／查詢／銷毀 |
  | `src/turnstile.js` | Turnstile siteverify |
  | `src/handlers/auth.js` | 註冊／登入／登出／查身分 |
  | `src/handlers/state.js` | 排程資料讀寫（樂觀鎖） |
  | `src/handlers/admin.js` | 帳號管理 API |
  | `src/handlers/share.js` | 跨帳號分享與操作記錄 API |
  | `tests/occurrence.test.mjs` | occurrence 引擎測試（`npm test`）|
  | `public/sw.js` | service worker（加到主畫面／離線可用）|
  | `schema.sql` | D1 資料表（`users`／`sessions`／`user_state`） |
  | `wrangler.jsonc` | Worker 設定與綁定 |

## 技術架構

- 前端為零依賴單頁應用，可獨立運作；後端（Cloudflare Worker + D1）為選用的漸進增強
- 資料存在 JavaScript 變數（`majorProjects`、`items`、`ganttProjects`、`dailyLogs`、`customHolidays`），並自動同步到瀏覽器 `localStorage`（key = `workSchedule.v1`）
- **所有變更即時存檔，重新整理不會遺失**。所有寫入都經由單一入口 `commit()`，儲存失敗時自動降級為記憶體模式（例如 Claude Artifact 沙盒會封鎖 localStorage），功能不受影響
- 部署到 Cloudflare 後，`commit()` 額外觸發背景雲端同步（1.5 秒 debounce）。localStorage 仍是主要儲存，因此畫面永遠即時且可離線使用
- 字型：JetBrains Mono（數字/標籤）＋ Noto Sans TC（中文內文），透過 Google Fonts CDN 載入
- 整體視覺：亮色系（warm paper 底色 + 白色卡片），色彩角色：
  - 🟢 teal（`--teal` #1F8C68）＝ 工作項目
  - 🟣 violet（`--violet` #6C5CE0）＝ 會議安排
  - 🟠 amber（`--amber` #C9822E）＝ 作業（自訂）＋ 介面主色 / 今日標示 / 循環標籤

---

## 資料模型

```
majorProjects = [{ id, name }]
// 大項目：純分類標籤，無日期，可更名、可刪除（刪除後底下項目變獨立項目）

items = [{
  id, title, type: 'work'|'meeting'|'assignment',
  parentId,           // 歸屬大項目 id，或 null（獨立項目）
  meetingTime,        // 選填時間，三種類型都可使用（欄位名為歷史沿革）
  date,               // 錨點日期（非循環時＝實際日期；循環時＝起始日期）
  recurrence: null | {
    freq: 'weekly'|'biweekly'|'monthly'|'quarterly',
    day,               // 每月/每季固定第幾天（每週類不使用，星期幾由 date 決定）
    holidayRule: 'none'|'postpone'|'advance',
    until              // 循環期限（選填，超過此日期不再產生）
  },
  done, doneMap,       // 完成狀態：非循環用 done；循環用 doneMap[occKey]
  overrides,           // occKey -> 該次覆寫日期
  skipped              // occKey -> true 表示略過該次
}]

ganttProjects = [{ id, name, notes, tasks: [{ id, name, start, end, progress, done }] }]
// 專案（甘特圖）：獨立於「大項目」之外的另一套資料，各自有任務清單與筆記欄位

dailyLogs = { 'YYYY-MM-DD': '文字內容' }
// 每日記錄：只在「日曆」頁點選日期後、下方安排區塊顯示，不出現在日曆格子上

customHolidays = Set('YYYY-MM-DD', ...)
// 自訂假日，會影響「循環設定」的假日順延/提前判斷（週六日已自動視為假日）
```

---

## 目前功能總覽

### 左側導覽四大頁籤
1. **項目安排**
2. **日曆**
3. **專案**
4. **共享**（部署後才有作用）

手機上這四格會固定在畫面底部，方便單手操作。

### 1. 項目安排
- **大項目**管理：頂部橫向 chip 清單，可新增、更名（inline modal）、刪除；無需日期
- **小項目**建立/編輯：標題、類型（工作／會議／作業三選一）、時間（選填，三種類型皆可）、歸屬大項目（可選獨立）、日期、循環設定
- 年度／季度／月份／特定日期四種篩選頁籤，各自可切換範圍瀏覽
- 搜尋框可依標題或大項目關鍵字過濾，另可依類型與「只看未完成」篩選（這些條件不存檔，重整回到預設）
- 每列顯示：勾選框、類型徽章、標題、所屬大項目標籤、循環徽章（含期限）、日期、編輯／刪除按鈕；循環項目額外有「調整本次日期」「略過此次」按鈕

### 2. 日曆
- 月曆網格，可切換上月/下月/回到今天
- **今日格子**：淡橘色底＋橘色外框醒目標示
- 會議：只顯示「時間＋主旨」一行文字，依時間排序，不顯示細節
- 工作／作業：checklist 打勾樣式直接顯示於格子中（工作綠色、作業橘色），可直接勾選完成
- 點擊日期後，下方展開該日完整項目卡片（可編輯/刪除/勾選）
- **每日記錄**：點選日期後才出現在最下方的文字備忘欄位，自動儲存，不顯示於日曆格子

### 3. 專案（甘特圖）
- 可建立多個大型專案（下拉選單切換，可更名/刪除，與「大項目」是不同的獨立資料）
- 選擇專案後顯示：
  - **甘特圖**：任務依開始/結束日期畫成時間軸長條，內含進度% 填色、今日標線；顏色依狀態變化（進行中／已完成／已過期）
  - **待辦事項表格**：任務名稱、開始日期、結束日期、進度（可直接輸入編輯，即時反映到甘特圖）、完成打勾
  - **代辦項目**：每個待辦底下可再列出子項目。一旦有子項目，該任務的進度就改由「完成幾項／共幾項」自動計算，進度欄變成唯讀顯示；母項目的勾選框此時代表「全部完成／全部取消」
  - **專案筆記**：可自由輸入文字備忘，自動儲存

### 頂部儀表板
- **當日未完成** / **當週未完成**：直接列出未完成項目標題（非數字），依類型上色圓點標示，超過 6 項顯示「+ 還有 N 項」
- **當日會議項目**：列出今天所有會議的時間＋主旨

### 提醒通知
- 開啟頁面約 0.7 秒後自動彈出右上角「今日待完成提醒」卡片
- 右上角 🔔 鈴鐺按鈕可隨時重新叫出，並有未完成數量角標

### 外觀
- 右上角 🌙／☀️ 可切換亮色與暗色，預設跟隨系統設定，選過之後會記住（此偏好只存在這台裝置，不跟著帳號同步）

### 循環規則
- 每月固定 N 號 / 每季固定 N 號（每 3 個月一次）
- 遇假日（週六日 + 自訂假日）可選：不調整／順延下一個工作天／提前至前一個工作天
- 可設定循環期限（超過後不再產生新的一次）
- 個別某一次可單獨覆寫日期，或整次略過（不影響其他週期）

---

## 使用者與權限（部署到 Cloudflare 後才啟用）

- 使用者以 email + 密碼自行註冊，註冊頁有 Turnstile 真人驗證
- **註冊後須經管理者核准才能使用**；未核准者登入時連 session 都拿不到，因此完全進不了站
- 角色分「使用者」與「管理者」。管理者可核准／拒絕／停用／刪除帳號、升降角色，但**看不到任何人的排程內容**
- 停用或刪除帳號會立即讓對方所有裝置登出
- 第一位管理者由 `ADMIN_EMAILS` 環境變數指定，該名單內的 email 註冊後自動成為已核准的管理者
- 每個帳號註冊後從**空白介面**開始（示範資料只在未登入的單機模式出現）

### 4. 共享

- 在任一小項目或專案上按 **🔗**，輸入對方的帳號 email 即可分享
- 權限分兩級：**可檢視**（唯讀）與**可操作**（可勾選完成、勾選代辦項目、改進度）
- 重新命名、改日期、刪除永遠只有擁有者能做
- 分享的是同一份資料而不是複本——對方看到的永遠是最新版本，擁有者也隨時可以收回
- 收到的分享集中在「共享」頁，不會混進自己的項目安排與日曆（見已知限制 6）

## 已知限制

1. **單機模式限單一瀏覽器**：未部署時資料存在 `localStorage`，重新整理不會遺失，但不跨裝置；清除瀏覽器資料會一併清掉。部署到 Cloudflare 後即可跨裝置同步，且兩台裝置衝突時會跳出提示讓你選擇保留哪一份，不會靜默覆蓋。
2. **假日判斷**：自動判斷週六、週日；國定假日可在「自訂假日」區一鍵載入內建清單（目前內建 2026 年，來源為行政院人事行政總處的辦公日曆表），其他年度可批次貼上。新年度公布後以 `tools/parse-gov-calendar.py` 解析官方 xlsx 即可補上。
3. **無法表達「週末補班日」**：週六日一律視為假日，若某年有補班，落在那天的循環仍會被順延（2026 年沒有補班日）。要支援的話需要新增「工作日覆蓋」的概念，不是把假日清單反過來用就好。
4. **甘特圖**：目前為靜態百分比定位，不支援拖曳調整長條/日期（需透過表格輸入日期修改）。
5. **變更循環頻率會重置單次紀錄**：每月 ↔ 每季互換時，該項目的完成／日期覆寫／略過紀錄會清空（儲存前會跳警告）。因為這些紀錄是以週期代碼（`YYYY-MM`／`YYYY-QN`）為索引，換頻率後索引即失效。
6. **檢視狀態不存檔**：目前頁籤、選取的年／季／月與搜尋篩選條件重整後回到預設，只有資料本身持久化。
7. **分享的項目只出現在「共享」頁**：不會併入自己的項目安排、日曆與頂部指標。這是刻意的——若混進自己的資料裡，每一條寫入路徑都得先判斷「這是不是別人的」，一旦漏判就會把別人的資料推進自己的雲端存檔。
8. **分享內容沒有離線快取**：它的權威來源是對方的帳號，離線時看不到也改不了。

---

## 接續開發建議

在新對話中，可以這樣開場：
> 「這是我之前跟你一起做的工作排程確認系統，附上 HTML 檔和說明文件，請先讀懂目前架構，接下來我要新增／修改 ___」

並同時上傳：
- `public/index.html`
- 本說明檔 `工作排程確認系統_專案說明.md`
