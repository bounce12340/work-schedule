{
  "name": "work-schedule",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute work-schedule-db --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute work-schedule-db --local --file=./schema.sql",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.118.0"
  }
}
